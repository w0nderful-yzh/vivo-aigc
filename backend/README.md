# Task 3 后端与 AI 服务

本目录由后端 / AI 接口开发者负责。

优先实现 Mock 模式：

1. `POST /api/ai/intervention`
2. `POST /api/ai/rest-chat`
3. `POST /api/ai/oral-review`
4. `POST /api/ai/report`

真实 vivo AIGC / 蓝心大模型接入不能阻塞 Demo。接口字段以 `docs/API.md` 为准。

